/**
 * Tuples est utilisé pour mettre une sorte de regle dans un tableau
 * 
 */

let tuple : [string, number, boolean]
/**
 * Le tableau doit suivre le pattern défini
 * i-e string, number, boolean
 */
tuple = ["test", 2, false]

type userTuple = [number, string]

const newUser: userTuple= [0, "test"]